# Mods 放置目录

把服务端所需的 `.jar` 模组放在这里，然后重启容器。

所有模组必须同时兼容：

- Minecraft `26.2`
- NeoForge（不能混用 Forge 或 Fabric 模组）
- Java 25

大多数内容型模组也必须安装到每位玩家的客户端。纯客户端模组不要放进服务端。

本目录是权威来源：容器每次启动都会将这里的模组同步到 `data/mods/`，从这里删除的旧 `.jar` 也会从服务端移除。
